/** Minimal translucent gold badge for Pro subscribers in the assistant header. */
export function ProBadge() {
  return (
    <span className="pro-badge select-none" aria-label="Pro plan">
      Pro
    </span>
  );
}

export default ProBadge;
